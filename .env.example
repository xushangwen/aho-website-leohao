# 后端服务地址（不含 /api 后缀）
# 复制此文件为 .env，填入对应环境的地址

# 本地开发（Docker）
# NUXT_BACKEND_URL=http://localhost:8080

# 测试服务器（宝塔，后端同机运行）
# NUXT_BACKEND_URL=http://127.0.0.1:8080

# 生产环境（按实际后端地址填写）
# NUXT_BACKEND_URL=https://api.yourdomain.com
